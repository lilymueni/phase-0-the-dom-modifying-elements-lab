const newHeader = document.querySelector('h1');

document.addEventListener('DOMContentLoaded', () => {
    var newHeader = document.getElementById('victory');
    newHeader.textContent = 'LILY-NZIOKI is the champion';
});